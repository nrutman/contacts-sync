#!/usr/bin/env php
<?php
if (!file_exists(dirname(__DIR__) . "/vendor/phpunit/phpunit/phpunit")) {
    echo "Unable to find PHPUnit in `vendor/phpunit/phpunit/`.\n";
    exit(1);
}

require dirname(__DIR__) . "/vendor/phpunit/phpunit/phpunit";

